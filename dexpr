use strict;
use warnings;
use Path::Tiny;
use Data::Dumper::Compact;

my $data = eval '+'.($ARGV[0] =~ /^[\{\[]/ ? $ARGV[0] : path($ARGV[0])->slurp);

die "Invalid data: $@" unless $data;

print Data::Dumper::Compact->dump($data);
